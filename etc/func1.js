// 매개변수가 없는 익명 함수
let hi = function () {
  console.log("hi");
}

// 매개변수가 없는 화살표 함수
let hi0 = () => {
  console.log("hi")
}

// 매개변수가 있는 익명 함수
let sum = function (x, y) {
  return x + y
}

// 매개변수가 있는 화살표 함수
let sum0 = (x, y) => x + y;